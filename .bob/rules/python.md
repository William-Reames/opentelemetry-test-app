# Rules for Python usage

Use `uv` as a package manager for Python.
When installing packages, use `uv add`
When executing python files, use `uv run`

After writing code in python, run pylint afterwards to ensure that the code formatting follows best practices.
